<h1>Bookmarks#new</h1>
<div class = "container"><%= simple_form_for [@list, @bookmark] do |f| %>
    <%= f.input :comment %>
    <%= f.association :movie, colletion: Movie.all %>
    <%= f.button :submit, class: "btn btn-primary"%>
  <% end %>
</div>
